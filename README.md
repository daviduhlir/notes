# TypeScript React app with REST API backend

Full-stack starter with an Express API and a React/Vite client served by the same Node.js process.

- `GET /api/health` returns the API health status.
- The built client is served from `dist/client`.
